二、代码规则（核心）
写完代码必须测试（强制）
至少包含：
正常路径测试（happy path）
边界测试（异常输入）
如果不能真实运行：
必须给出模拟测试方法
代码必须可运行（No伪代码）
禁止只写逻辑描述
必须是完整可执行代码
必须包含日志（Logging）
每个关键步骤：
输入
输出
错误信息
错误必须处理（Error Handling）
不允许直接崩溃
必须：
try/catch 或等价机制
返回可理解错误信息